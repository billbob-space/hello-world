# Genere par init.sh depuis fabrique.yml et apps/*/app.yml.
# NE PAS EDITER — ./init.sh --check refuse un compose desynchronise.
#
# Une seule stack dockhand. Le rayon de souffle est commun : une erreur dans un
# bloc fait echouer le deploiement de toute la fabrique.
#
# TROIS SORTES DE SERVICES cohabitent ici, dans un espace de noms plat, et UNE
# SEULE EST ROUTEE :
#
#   <app>          l'application elle-meme, decrite par apps/<app>/app.yml.
#                  Seule sorte a porter des labels de ROUTAGE : un routeur, une
#                  URL https://<app>.apps.billbob.ovh, un middleware
#                  d'authentification Google et priority=100.
#   <app>-<nom>    service annexe prive d'une application (section services:
#                  de son app.yml). Un seul label : traefik.enable=false.
#   <nom>          service partage par plusieurs applications (shared_services
#                  dans fabrique.yml). Le meme unique label.
#
# Toutes vivent sur le meme reseau apps_net et se joignent entre elles par
# leur nom de service ; aucune ne publie de port sur l'hote. Ce qui expose un
# service a Internet, ce sont ses labels Traefik, pas le reseau — mais l'absence
# de label n'est PAS une protection : avec exposedByDefault, qui est le DEFAUT,
# un conteneur sans le moindre label recoit quand meme un routeur, donc une URL,
# et sans middleware d'authentification. Seul traefik.enable=false l'en retire ;
# c'est pourquoi il est pose sur chaque service non route ci-dessous.
#
# Les points de montage sont des VOLUMES NOMMES Docker, jamais des bind mounts.
# Un volume s'appelle <proprietaire>-<nom> — le proprietaire etant l'app ou le
# service partage qui le declare — et « docker compose up » le cree tout seul :
# aucune action prealable sur l'hote, pour aucune app, jamais. Au premier
# montage, Docker recopie dans le volume vide le contenu du repertoire tel qu'il
# existe dans l'IMAGE : C'EST DONC LE DOCKERFILE QUI FIXE LE PROPRIETAIRE. Une
# app en non-root doit creer son repertoire et le chown AVANT sa directive
# USER, sinon le volume appartient a root et elle ne peut pas y ecrire.
services:

  # >>> ardoise — apps/ardoise/app.yml — https://ardoise.apps.billbob.ovh
  ardoise:
    image: ghcr.io/billbob-space/hello-world/ardoise:01acc5a7aed9b6d63cf48341db71e1ca8a855c8b
    container_name: ardoise
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.ardoise.rule=Host(`ardoise.apps.billbob.ovh`)"
      - "traefik.http.routers.ardoise.entrypoints=websecure"
      - "traefik.http.routers.ardoise.priority=100"
      # forwardauth = authentification Google + liste blanche du serveur
      # (exposure private). La retirer exposerait l'app en clair.
      - "traefik.http.routers.ardoise.middlewares=forwardauth,security-headers@file"
      - "traefik.http.routers.ardoise.tls.certresolver=letsencrypt"
      - "traefik.http.services.ardoise.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-}
    # Services partages dont cette app depend (needs: dans app.yml).
    depends_on: [redis]
  # <<< ardoise

  # >>> ardoise-base — service annexe de ardoise — apps/ardoise/app.yml
  ardoise-base:
    image: postgres:17-alpine
    container_name: ardoise-base
    restart: unless-stopped
    mem_limit: 192m
    # Le tag est souvent mutable : sans ce reglage, un redeploiement relance
    # l'image locale deja presente et sert silencieusement la version d'avant.
    pull_policy: always
    # Service NON ROUTE : aucune URL, aucun middleware a oublier. Il vit sur
    # apps_net, ou ses voisins le joignent par « ardoise-base ». Aucun port publie non
    # plus. Ce n'est PAS l'absence de label qui l'en retire — voir « labels: »
    # plus bas, qui est la seule chose a le faire.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks: [apps_net]
    # LE label qui compte sur un service non route : c'est le seul qui RETIRE du
    # routage. Sans lui, exposedByDefault suffit a creer un routeur sans
    # authentification, et un LABEL traefik.* grave dans l'image publierait une
    # route que ce fichier ne peut pas ecraser. Inoffensif si le serveur pose
    # deja exposedByDefault: false, indispensable sinon.
    labels:
      - "traefik.enable=false"
    volumes:
      - ardoise-donnees:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-}
  # <<< ardoise-base

  # >>> cadran — apps/cadran/app.yml — https://cadran.apps.billbob.ovh
  cadran:
    image: ghcr.io/billbob-space/hello-world/cadran:01acc5a7aed9b6d63cf48341db71e1ca8a855c8b
    container_name: cadran
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.cadran.rule=Host(`cadran.apps.billbob.ovh`)"
      - "traefik.http.routers.cadran.entrypoints=websecure"
      - "traefik.http.routers.cadran.priority=100"
      # forwardauth = authentification Google + liste blanche du serveur
      # (exposure private). La retirer exposerait l'app en clair.
      - "traefik.http.routers.cadran.middlewares=forwardauth,security-headers@file"
      - "traefik.http.routers.cadran.tls.certresolver=letsencrypt"
      - "traefik.http.services.cadran.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
  # <<< cadran

  # >>> compteur — apps/compteur/app.yml — https://compteur.apps.billbob.ovh
  compteur:
    image: ghcr.io/billbob-space/hello-world/compteur:01acc5a7aed9b6d63cf48341db71e1ca8a855c8b
    container_name: compteur
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.compteur.rule=Host(`compteur.apps.billbob.ovh`)"
      - "traefik.http.routers.compteur.entrypoints=websecure"
      - "traefik.http.routers.compteur.priority=100"
      # forwardauth-open = tout compte Google, login obligatoire
      # (exposure google). La retirer exposerait l'app en clair.
      - "traefik.http.routers.compteur.middlewares=forwardauth-open,security-headers@file"
      - "traefik.http.routers.compteur.tls.certresolver=letsencrypt"
      - "traefik.http.services.compteur.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-}
    # Services partages dont cette app depend (needs: dans app.yml).
    depends_on: [redis]
  # <<< compteur

  # >>> compteur-base — service annexe de compteur — apps/compteur/app.yml
  compteur-base:
    image: postgres:17-alpine
    container_name: compteur-base
    restart: unless-stopped
    mem_limit: 160m
    # Le tag est souvent mutable : sans ce reglage, un redeploiement relance
    # l'image locale deja presente et sert silencieusement la version d'avant.
    pull_policy: always
    # Service NON ROUTE : aucune URL, aucun middleware a oublier. Il vit sur
    # apps_net, ou ses voisins le joignent par « compteur-base ». Aucun port publie non
    # plus. Ce n'est PAS l'absence de label qui l'en retire — voir « labels: »
    # plus bas, qui est la seule chose a le faire.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks: [apps_net]
    # LE label qui compte sur un service non route : c'est le seul qui RETIRE du
    # routage. Sans lui, exposedByDefault suffit a creer un routeur sans
    # authentification, et un LABEL traefik.* grave dans l'image publierait une
    # route que ce fichier ne peut pas ecraser. Inoffensif si le serveur pose
    # deja exposedByDefault: false, indispensable sinon.
    labels:
      - "traefik.enable=false"
    volumes:
      - compteur-donnees:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-}
  # <<< compteur-base

  # >>> estran — apps/estran/app.yml — https://estran.apps.billbob.ovh
  estran:
    image: ghcr.io/billbob-space/hello-world/estran:7c18d32a156de19364b3b9b371d31d8313c86b7c
    container_name: estran
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.estran.rule=Host(`estran.apps.billbob.ovh`)"
      - "traefik.http.routers.estran.entrypoints=websecure"
      - "traefik.http.routers.estran.priority=100"
      # forwardauth = authentification Google + liste blanche du serveur
      # (exposure private). La retirer exposerait l'app en clair.
      - "traefik.http.routers.estran.middlewares=forwardauth,security-headers@file"
      - "traefik.http.routers.estran.tls.certresolver=letsencrypt"
      - "traefik.http.services.estran.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
    environment:
      - API_MAREE_KEY=${API_MAREE_KEY:-}
  # <<< estran

  # >>> hello-world — apps/hello-world/app.yml — https://hello-world.apps.billbob.ovh
  hello-world:
    image: ghcr.io/billbob-space/hello-world/hello-world:7ba3e0bbafcfa929a7810110bc11b2afd4051fb5
    container_name: hello-world
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.hello-world.rule=Host(`hello-world.apps.billbob.ovh`)"
      - "traefik.http.routers.hello-world.entrypoints=websecure"
      - "traefik.http.routers.hello-world.priority=100"
      # forwardauth = authentification Google + liste blanche du serveur
      # (exposure private). La retirer exposerait l'app en clair.
      - "traefik.http.routers.hello-world.middlewares=forwardauth,security-headers@file"
      - "traefik.http.routers.hello-world.tls.certresolver=letsencrypt"
      - "traefik.http.services.hello-world.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
  # <<< hello-world

  # >>> marcq-handball — apps/marcq-handball/app.yml — https://marcq-handball.apps.billbob.ovh
  marcq-handball:
    image: ghcr.io/billbob-space/hello-world/marcq-handball:37eb474fd85452aa736ed385d17334c4e04fb4fb
    container_name: marcq-handball
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.marcq-handball.rule=Host(`marcq-handball.apps.billbob.ovh`)"
      - "traefik.http.routers.marcq-handball.entrypoints=websecure"
      - "traefik.http.routers.marcq-handball.priority=100"
      # public = AUCUNE authentification (exposure public). Acces anonyme,
      # rate-limit seul. Ne pas confondre avec forwardauth-open, qui exige un
      # compte Google. X-Forwarded-User n'est PAS pose sur ce palier.
      - "traefik.http.routers.marcq-handball.middlewares=public,security-headers@file"
      - "traefik.http.routers.marcq-handball.tls.certresolver=letsencrypt"
      - "traefik.http.services.marcq-handball.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
    volumes:
      - marcq-handball-donnees:/var/lib/marcq-handball
  # <<< marcq-handball

  # >>> pilabelle — apps/pilabelle/app.yml — https://pilabelle.apps.billbob.ovh
  pilabelle:
    image: ghcr.io/billbob-space/hello-world/pilabelle:e127bd32b063aa9afe177585b3c557a453634104
    container_name: pilabelle
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.pilabelle.rule=Host(`pilabelle.apps.billbob.ovh`)"
      - "traefik.http.routers.pilabelle.entrypoints=websecure"
      - "traefik.http.routers.pilabelle.priority=100"
      # forwardauth = authentification Google + liste blanche du serveur
      # (exposure private). La retirer exposerait l'app en clair.
      - "traefik.http.routers.pilabelle.middlewares=forwardauth,security-headers@file"
      - "traefik.http.routers.pilabelle.tls.certresolver=letsencrypt"
      - "traefik.http.services.pilabelle.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
    volumes:
      - pilabelle-donnees:/var/lib/pilabelle
    environment:
      - VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY:-}
      - VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY:-}
      - VAPID_CONTACT=${VAPID_CONTACT:-}
  # <<< pilabelle

  # >>> ramure — apps/ramure/app.yml — https://ramure.apps.billbob.ovh
  ramure:
    image: ghcr.io/billbob-space/hello-world/ramure:01acc5a7aed9b6d63cf48341db71e1ca8a855c8b
    container_name: ramure
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.ramure.rule=Host(`ramure.apps.billbob.ovh`)"
      - "traefik.http.routers.ramure.entrypoints=websecure"
      - "traefik.http.routers.ramure.priority=100"
      # forwardauth = authentification Google + liste blanche du serveur
      # (exposure private). La retirer exposerait l'app en clair.
      - "traefik.http.routers.ramure.middlewares=forwardauth,security-headers@file"
      - "traefik.http.routers.ramure.tls.certresolver=letsencrypt"
      - "traefik.http.services.ramure.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
  # <<< ramure

  # >>> renaissance-gym — apps/renaissance-gym/app.yml — https://renaissance-gym.apps.billbob.ovh
  renaissance-gym:
    image: ghcr.io/billbob-space/hello-world/renaissance-gym:a9a22b9d96656baaa8b518fb2297c12f218c516e
    container_name: renaissance-gym
    restart: unless-stopped
    mem_limit: 128m
    # Le tag vient de versions.yml, ou la CI ecrit le commit qu'elle vient de
    # publier. Il change donc a chaque livraison de CETTE app, et de celle-la
    # seule : « docker compose up » ne recree que les services dont la ligne
    # image: a bouge. Une app encore absente de versions.yml retombe sur le tag
    # mutable « :main », et c'est le seul cas ou pull_policy sert vraiment —
    # sans lui, un redeploiement relancerait l'image locale, donc la version
    # precedente. Sur un tag epingle il ne coute qu'une verification d'empreinte.
    pull_policy: always
    # Aucun port publie : Traefik joint le conteneur par le reseau apps_net.
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Journaux bornes : le disque du serveur est a 92 %, et la fabrique
    # multiplie les services. Un json-file non borne les remplirait.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # priority=100 est OBLIGATOIRE : un routeur catch-all capte tout
      # *.apps.billbob.ovh par HostRegexp, et sa regle est plus longue que ce
      # Host(). Traefik departageant par longueur de regle, il gagnerait et
      # servirait un 404 silencieux.
      - "traefik.enable=true"
      - "traefik.http.routers.renaissance-gym.rule=Host(`renaissance-gym.apps.billbob.ovh`)"
      - "traefik.http.routers.renaissance-gym.entrypoints=websecure"
      - "traefik.http.routers.renaissance-gym.priority=100"
      # public = AUCUNE authentification (exposure public). Acces anonyme,
      # rate-limit seul. Ne pas confondre avec forwardauth-open, qui exige un
      # compte Google. X-Forwarded-User n'est PAS pose sur ce palier.
      - "traefik.http.routers.renaissance-gym.middlewares=public,security-headers@file"
      - "traefik.http.routers.renaissance-gym.tls.certresolver=letsencrypt"
      - "traefik.http.services.renaissance-gym.loadbalancer.server.port=8080"
      - "traefik.docker.network=apps_net"
    networks: [apps_net]
    volumes:
      - renaissance-gym-donnees:/var/lib/renaissance-gym
  # <<< renaissance-gym

  # ===== services partages — fabrique.yml, cle shared_services =====
  # Un seul exemplaire pour toute la fabrique, joignable par son nom de service
  # depuis n'importe quelle app d'apps_net, et jamais route vers l'exterieur.
  # Une app declare sa dependance avec « needs: » dans son app.yml.

  # >>> redis — service partage — fabrique.yml
  redis:
    image: valkey/valkey:8-alpine
    container_name: redis
    restart: unless-stopped
    mem_limit: 96m
    # Le tag est souvent mutable : sans ce reglage, un redeploiement relance
    # l'image locale deja presente et sert silencieusement la version d'avant.
    pull_policy: always
    # Service NON ROUTE : aucune URL, aucun middleware a oublier. Il vit sur
    # apps_net, ou ses voisins le joignent par « redis ». Aucun port publie non
    # plus. Ce n'est PAS l'absence de label qui l'en retire — voir « labels: »
    # plus bas, qui est la seule chose a le faire.
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks: [apps_net]
    # LE label qui compte sur un service non route : c'est le seul qui RETIRE du
    # routage. Sans lui, exposedByDefault suffit a creer un routeur sans
    # authentification, et un LABEL traefik.* grave dans l'image publierait une
    # route que ce fichier ne peut pas ecraser. Inoffensif si le serveur pose
    # deja exposedByDefault: false, indispensable sinon.
    labels:
      - "traefik.enable=false"
    command: ["valkey-server", "--maxmemory", "64mb", "--maxmemory-policy", "allkeys-lru", "--save", ""]
  # <<< redis

# Volumes nommes montes par les services ci-dessus. Docker les cree au premier
# « compose up » et les conserve entre deux deploiements. Leur contenu initial,
# proprietaire compris, est celui du repertoire dans l'image : rien n'est a
# preparer sur l'hote, mais le Dockerfile doit chown le chemin avant son USER.
#
# « name: » est OBLIGATOIRE sous chaque entree. Sans lui, Compose prefixe le nom
# du projet et le volume REEL s'appelle <projet>_<nom> : une sauvegarde lancee
# sur le nom court monterait alors un volume que Docker viendrait de creer VIDE,
# tar archiverait un repertoire vide et la commande sortirait en 0. La stack
# etant unique, le prefixe de projet n'apporte rien — on le retire, et le nom
# reel redevient egal au nom documente.
volumes:
  ardoise-donnees:
    name: ardoise-donnees
  compteur-donnees:
    name: compteur-donnees
  marcq-handball-donnees:
    name: marcq-handball-donnees
  pilabelle-donnees:
    name: pilabelle-donnees
  renaissance-gym-donnees:
    name: renaissance-gym-donnees

networks:
  apps_net:
    external: true
